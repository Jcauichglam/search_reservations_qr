import { Format12hoursPipe } from './format12hours.pipe';

describe('Format12hoursPipe', () => {
  it('create an instance', () => {
    const pipe = new Format12hoursPipe();
    expect(pipe).toBeTruthy();
  });
});
